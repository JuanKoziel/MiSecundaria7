# Pendientes del Testeo Masivo — Mi Secundaria 7

Resultado del testeo masivo completo (roles Alumno, Familia, Docente, Preceptor, Jefe de Preceptores, Administrador, Director y pruebas transversales). Cada punto indica qué falla, qué se espera y dónde está el componente afectado.

**Leyenda de estado:**
- ✅ = Resuelto en cambios sin commitear
- ❌ = Pendiente

---

## CRÍTICO — No funciona nada / crashea la página

| # | Rol | Problema | Dónde | Qué se espera | Estado |
|---|---|---|---|---|---|
| C1 | **Todos** | **"Marcar todas como leídas" no funciona** en ningún usuario (Alumno, Familia, Preceptor, Admin, Director, Jefe) | Backend `views.py` (endpoint marcar_todas_leidas) + `Notificaciones.jsx` | Que marque todas las notificaciones como leídas y actualice el contador a 0 | ✅ Backend corregido (`id_notificacion__in`). Verificar en frontend. |
| C2 | **Preceptor** | Botón **"Nuevo Tutor"** cuelga la página (ReferenceError: renderFormTutor is not defined) | `Preceptores/tutores.jsx` | Que abra el formulario de creación de tutor | ✅ Agregado `renderFormTutor` + `TutoresAlumnosEditor` |
| C3 | **Preceptor** | Botón **"Nuevo Docente"** cuelga la página | `Preceptores/docentes.jsx` | Que abra el formulario de creación de docente | ✅ Agregado `AsignacionesEditor` |
| C4 | **Admin/Jefe** | **Crear jefe de preceptores** muestra "id_usuario_existente: Introduzca un número entero válido" | `serializers.py` (5 serializers afectados) | Que al enviar un usuario existente lo asocie correctamente | ✅ `EnteroOpcionalField` creado y aplicado en todos los serializers |
| C5 | **Admin/Docente** | **Archivos de planificación**: descargar/ver tira **404 Not Found** | `Profesores/PanelPlanif.jsx`, backend (ruta de archivos media/) | Que el archivo se pueda descargar o ver | ✅ Arreglado y verificado: la causa era que `services/api.js` exportaba `BASE_URL = VITE_API_URL` = `http://localhost:8000/api` (sin depurar el sufijo), y todos los links de archivos se construyen `${API_BASE}${ruta_archivo}` donde `ruta_archivo` ya contiene `/media/...` → terminaba en `http://localhost:8000/api/media/...` → 404. `BASE_URL` ahora se deriva de `API_BASE` sin el sufijo `/api` → `http://localhost:8000/media/...` responde 200 (verificado con GET real contra runserver para planificaciones con y sin caracteres acentuados). |
| C6 | **Admin/Preceptor** | **Archivos de actividades y proyectos**: descargar/ver tira **404 Not Found** | `Profesores/PanelActividades.jsx`, `Preceptores/Proyectos.jsx`, backend | Que el archivo se pueda descargar o ver | ✅ Arreglado y verificado: mismo fix que C5 (la corrección en `services/api.js` es central, beneficia a actividades_docentes, actas, comunicados, ddjj y libro_temas, todos los cuales guardan `ruta_archivo` con prefijo `/media/`). Verificado con GET real contra runserver: `/media/actividades_docentes/*.pdf`, `/media/actas/*.pdf`, `/media/comunicados/*.pdf`, `/media/libro_temas/*.pdf` y `/media/ddjj_docentes/*.docx` responden 200. |
| C7 | **Admin** | **Boletines**: al seleccionar otra materia **vuelve automáticamente a la anterior** | `Administracion/notas.jsx` (selector de materia) | Que el selector de materia funcione y muestre las notas de la materia elegida | ✅ Arreglado y build OK: la causa estaba en `Shared/FiltrosAnioCurso.jsx` (`FiltrosAutonomo`): los efectos de sincronización incluían `onCursoChange`/`onCursoObjChange` en sus dependencias, y como el padre (`Administracion/notas.jsx`) crea `handleCursoChange` nuevo en cada render, cada selección de materia provocaba un re-render que re-disparaba el efecto → `setMateria(materias[0])` → la materia volvía a la primera. Ahora los callbacks del padre se guardan en refs (`useRef`) y los efectos solo dependen de `cursosObj` y los selectores año/división, por lo que no se re-disparan por cambios de identidad. Build OK. |

---

## ALTA — Funcionalidad rota / no se puede usar

| # | Rol | Problema | Dónde | Qué se espera | Estado |
|---|---|---|---|---|---|
| A1 | **Todos** | **Bug de multi-rol**: si el usuario tiene varios roles (ej. director + familia + docente), al entrar con un rol le llegan las notificaciones de **todos** sus roles, no solo del rol activo | Backend `notifications.py` + `views.py` (NotificacionViewSet.get_queryset) | Que solo lleguen notificaciones del rol con el que está logueado actualmente | ✅ Backend: notificaciones etiquetadas con segmento (rol), filtro por rol_activo en get_queryset. Frontend no requiere cambios (el backend ya filtra). |
| A2 | **Alumno/Familia** | **Asistencias**: al entrar a Asistencias, antes de seleccionar materia debería mostrar una **tablita resumen** con las últimas asistencias de cada materia | `AlumnoDashboard.jsx`, `Familia/Asistencias.jsx` (sección asistencias) | Tabla resumen de asistencias por materia antes del detalle. Familia: ídem pero con selector de hijo. | ✅ Agregada "Últimas asistencias por materia" en `AsistenciasUnificada.jsx` (usa el mismo componente Alumno y Familia). Filas clicables para ver el detalle. |
| A3 | **Docente** | **Comunicados**: el selector de materia **no filtra bien**. Si tengo seleccionado Matemática, muestra comunicados de Prácticas del Lenguaje también. Si no hay nada seleccionado, muestra comunicados igual (no debería) | `Profesores/PanelInfo.jsx`, `Profesores/TopHeader.jsx` (selector global) | Que el comunicado se filtre por la materia seleccionada; si no hay selección, no mostrar comunicados de materia | ✅ `ComunicadosView.jsx`: ahora recibe `materiaSeleccionada`, filtra por curso+materia y no muestra nada si no hay curso seleccionado. `PanelProfesores.jsx` pasa `materiaSeleccionada`. |
| A4 | **Docente** | **Actividades de materias adeudadas**: no hay forma de subir/crear actividades para previas en el apartado de materias adeudadas. Tampoco se ven en Alumno ni Familia. | `Profesores/PanelMateriasAdeudadasDocente.jsx` | Que el docente pueda crear actividades para materias adeudadas (previas) y que se vean en Alumno/Familia | ✅ `PanelMateriasAdeudadasDocente.jsx`: selector de tipo Intensificación/Previa, sin período para previas, badge de tipo en la tabla. `ActividadesView.jsx`: pestaña "Materias adeudadas" con las actividades (intensificación y previas) agrupadas por materia para Alumno/Familia. |
| A5 | **Docente** | **Planificaciones**: falta botón de **"Marcar como verificado"** en admin/director. También falta un botón de descarga/ver que funcione | `Administracion/` (panel de planificaciones), `Profesores/PanelPlanif.jsx` | Botón "Marcar como verificado" visible para admin/director + notificación al docente + archivo descargable | ✅ Backend: acción `POST /planificaciones/{id}/verificar/` (solo admin/director) que setea `estado='Verificado'` y notifica al docente (`_notificar_planificacion_verificada`). Frontend: `Administracion/docentes.jsx` agrega badge de estado y botón "Marcar como verificado" junto a "Ver proyecto" (ambos roles usan el mismo AdminDashboard). |
| A6 | **Docente** | **DDJJ**: falta botón de **"Marcar como verificado"** en admin/director | `Administracion/` (panel de DDJJ), `Profesores/PanelPlanif.jsx` | Botón "Marcar como verificado" + notificación al docente de que fue verificada | ✅ Migración `0005_ddjj_docente_verificada` (columna `verificada` en `ddjj_docente`), acción `POST /ddjj-docente/{id}/verificar/` (solo admin/director) + `_notificar_ddjj_verificada`. Serializer de Docente expone `ddjj_verificada`. `Administracion/docentes.jsx`: badge "Verificada" y botón "Marcar verificado" en la fila y en el modal. |
| A7 | **Docente** | **Carga masiva de notas/asistencias**: al admin/director **no le llega la notificación** de la carga masiva | `backend/proyecto/escuela/notifications.py` (evento de carga masiva), `Profesores/PanelAlumnos.jsx` | Que admin/director reciba "Notas cargadas" / "Asistencias cargadas" cuando un docente hace carga masiva | ✅ Verificado: no hay endpoint "carga masiva" dedicado; el flujo usa `createCalificacion`/`updateCalificacion` individuales (PanelAlumnos usa `Promise.all`), ambos llaman `_notificar_carga_notas` → notifica `_usuarios_directivos()` + preceptores. Asistencias idéntico con `_notificar_carga_asistencias`. Las funciones de notificación existen y están correctamente cableadas. Verificar con testeo real para descartar race condition por requests simultáneos. |
| A8 | **Preceptor** | **Horarios**: al guardar un horario (crear/editar) da **error**. Tampoco permite crear horarios de Educación Física | `permissions.py` (PuedeGestionarHorarios) + `views.py` (HorarioViewSet) | Que el preceptor pueda crear/editar horarios de sus cursos sin error | ✅ Permiso `PuedeGestionarHorarios` creado y aplicado a ambos viewsets |
| A9 | **Preceptor** | **Actas**: si el preceptor creó un acta, debería poder **editarla/borrarla**, pero no puede | `Preceptores/actas.jsx` | Que el creador del acta pueda editarla y borrarla | ✅ Verificado: el control de propiedad ya existe y es correcto. Backend: `perform_update`/`perform_destroy` de `ActaViewSet` y `ActaRelacionMixin` llaman `_check_acta_owner_permission` (permite al creador vía `id_usuario_creador`). Frontend: los botones editar/eliminar se muestran solo si `acta.id_usuario_creador === user.id` (DataContext preserva `id_usuario_creador`). DB: las actas tienen creador (incl. `preceptor_1_prueba`=1010, `jefe_preceptores_prueba`=1005). El flujo pedido ya está implementado; re-testear para confirmar (reporte previo pudo ser con lógica anterior). |
| A10 | **Preceptor** | **Agregar rol** en tutores: el botón no responde | `Preceptores/tutores.jsx` (botón AgregarRolModal) | Que el botón "Agregar rol" abra el modal y funcione | ✅ Arreglado + verificado: flujo backend completo probado vía API como `preceptor_1_prueba` (login → `/api/usuarios/sin-rol/?rol=familia` devuelve 61 candidatos → `/api/alumnos/` devuelve alumnos del curso → `POST /api/padres-tutores/` con `id_usuario_existente` responde 201). Causa probable en frontend: el botón "Agregar rol" abría el modal sin recargar la lista de candidatos (solo se cargaba al montar), a diferencia de "Quitar rol" y del patrón de los paneles de Administración. Se agregó `cargarPersonasSinRol()` al hacer click y tras asignar. Build OK. |
| A11 | **Preceptor** | **DNI duplicado**: al crear un alumno con DNI existente, muestra error de duplicado sin mensaje clara | `Preceptores/alumnos.jsx`, `backend/.../serializers.py` (AlumnoSerializer) | Mensaje de error claro: "Ya existe un alumno con ese DNI" | ✅ Arreglado + verificado: el mensaje genérico de DRF ("Ya existe un/a alumno con este/a dni.") provenía del `UniqueValidator` automático que corría antes del `validate_dni`. Se desactivó (`extra_kwargs['dni'] = {'validators': []}`) y se agregó `validate_dni` en `AlumnoSerializer` que compara DNIs normalizados (`normalizar_dni`) contra todos los alumnos (la DB tiene DNIs con y sin puntos), excluyendo la fila actual en update, y responde `{'dni': ['Ya existe un alumno con ese DNI.']}`. Verificado vía API como preceptor (400 con el mensaje claro). Frente: `mensajeError` ya muestra "dni: Ya existe un alumno con ese DNI.". Build/check OK. |
| A12 | **Admin** | **Alumnos**: el admin solo puede **ver** los estudiantes, no tiene botones de acción (editar, deshabilitar, borrar) | `Administracion/alumnos.jsx` | Que el admin tenga botones de acción como en los demás roles (editar, deshabilitar, borrar) | ✅ Arreglado + build OK: `Administracion/alumnos.jsx` era una lista de solo lectura ("Ver actas"). Se reemplazó por el componente de gestión completo de Preceptores (`Preceptores/alumnos.jsx`, `readOnly=false`) que ya incluye "Nuevo Estudiante" y botones Editar, Deshabilitar/Habilitar, Programar y Eliminar; el backend ya permite admin para esas rutas (`PuedeGestionarPersonas`). El admin conserva la gestión de actas en su propia sección "Actas". Nota: el listado ahora requiere seleccionar año + curso (como el resto de los paneles de administración), igual que en Preceptor. |
| A13 | **Admin** | **Docentes**: no tiene botones de acción (editar, deshabilitar, borrar). Falta marcar DDJJ como verificada | `Administracion/docentes.jsx` | Acciones de edición + verificación de DDJJ | ✅ Arreglado + build OK: se agregó CRUD completo a `Administracion/docentes.jsx` (botón "Nuevo Docente", y por fila Editar, Deshabilitar/Habilitar y Eliminar, con modal de formulario reutilizando `FormModal` y `createDocente`/`updateDocente`/`deleteDocente`). El backend ya permite admin para esas rutas (`PuedeGestionarPersonas`; `_require_preceptor_access` solo restringe preceptor). La verificación de DDJJ ya había quedado resuelta con A6 (botón "Marcar verificado" + badge "Verificada" en la misma pantalla). |
| A14 | **Admin** | **Jefe de Preceptores**: al crear nuevo jefe da error "id_usuario_existente" (ídem C4). Además la lista **no muestra correctamente** los jefes existentes | `Administracion/preceptores.jsx` | Que funcione la creación y que la lista muestre correctamente los jefes de preceptores | ✅ Verificado + cubierto por C4: `PreceptorSerializer.id_usuario_existente` usa `EnteroOpcionalField` (tolera `''`/null/ausente → None). Prueba real vía API como `admin_prueba`: login OK, `GET /api/preceptores/?rol=jefe_preceptores` devuelve los jefes correctamente (usuario, estado, id), `POST /api/preceptores/?rol=jefe_preceptores` con `id_usuario_existente:''` responde **201** (sin error de "número entero válido"). `AdminDashboard.jsx` ya monta `<Preceptores rol="jefe_preceptores">` para la pestaña de jefes. Datos de prueba eliminados tras el test. |
| A15 | **Admin** | **Cursos inactivos**: al mostrar registros inactivos, no da opción de **reactivar** | `Administracion/cursos.jsx` | Botón "Reactivar" para cursos inactivos | ✅ Arreglado + build OK: se agregó botón "Reactivar" (btn-success con check) en la celda de acciones cuando `c.activo === false`; llama `updateCurso(id, { activo: true })` y refresca. |
| A16 | **Admin** | **Asistencias**: el admin solo debería poder **ver** las asistencias, no modificar justificada/retiro | `Administracion/asistencias.jsx` | Modo solo lectura para admin (sin botones de editar justificación/retiro) | ✅ Arreglado + build OK: el tab "Asistencia por materia" mostraba el checkbox editable "Justificado" a pesar del badge "Solo lectura". Se reemplazó por badges estáticos ("Justificado"/"No justificado"). El backend ya rechazaba a admin (`justificar` de AsistenciaViewSet devuelve 403 si no tiene rol 'preceptor'). |
| A17 | **Jefe** | **Asignación de cursos**: al asignar un curso que ya tiene preceptor, da **error de permisos** en vez de un mensaje claro | `JefePreceptores/AsignacionCursos.jsx` | Mensaje: "Este curso ya tiene un preceptor asignado. Desasígnelo primero." | ✅ Arreglado + verificado: el backend bloqueaba a los jefes en cualquier UPDATE de cursos (`CursoViewSet` usaba `IsAdminOrDirectorForWrite`, solo admin/director) → 403 genérico inclusive para asignar a cursos libres. Nuevo permiso `PuedeGestionarCurso` (mantiene admin/director para todo; jefes solo PUT/PATCH), y en `perform_update` los jefes solo pueden cambiar `id_preceptor`; si el curso ya tiene otro preceptor responde 403 con **"Este curso ya tiene un preceptor asignado. Desasígnelo primero."**. En el frontend se valida antes de llamar a la API. Prueba vía API como `jefe_preceptores_prueba`: asignar a curso libre → 200, asignar a curso con preceptor → 403 + mensaje claro, desasignar → 200, modificar otro campo → 403. |
| A18 | **Jefe** | **Crear preceptor nuevo**: error "id_usuario_existente: Introduzca un número entero válido" (ídem C4) | `JefePreceptores/AdministracionPreceptores.jsx` | Que funcione la creación | ✅ (resuelto por C4/EnteroOpcionalField) |
| A19 | **Jefe** | **Actas de curso**: muestra actas de **estudiantes** también, solo debería mostrar actas de curso | `JefePreceptores/` (sección actas) | Filtrar para mostrar solo actas de curso (E21), no actas de alumno (E4) | ✅ Arreglado + build OK: se agregó el prop `onlyCursos` al componente compartido `Preceptores/actas.jsx` (sin efecto para admin/preceptor). La Jefatura (`JefePreceptorDashboard`) renderiza `<Actas ... onlyCursos />`: oculta las secciones "Actas de Estudiantes" y "Actas de Docentes", el formulario solo permite tipo "Curso" (y lo preselecciona), y la lista de "Actas de Curso" excluye las actas de alumno que también se linkean al curso (`actasAlumno.some(aa => aa.actaId === a.actaId)`). |
| A20 | **Jefe** | **Comunicados**: al crear/guardar comunicado, el formulario **no se cierra** automáticamente. También falta mostrar el **autor** del comunicado | `JefePreceptores/ComunicadosJefe.jsx` | Que al guardar se cierre el formulario + mostrar autor | ✅ Arreglado + verificado: (1) el cierre tardaba 2s vía `setTimeout`; ahora cierra el modal inmediatamente tras guardar (mensaje, form y archivos se limpian). (2) El autor no se mostraba porque `DataContext` no exponía `id_usuario_creador` en el map de comunicados y `creador_nombre` devolvía el username. Se agregó `id_usuario_creador` al map; `ComunicadoSerializer.get_creador_nombre` ahora resuelve el nombre completo (preceptor → docente → directivo → alumno → familiar); `ComunicadosView` usa `creador_nombre` con fallback `getNombreAutor` (que ahora también busca en `administradores`), en la tabla y el detalle. Verificado vía API: `creador_nombre` devuelve "Benítez, Carolina" (jefe uid 1005) y "Prueba, Admin" (admin uid 1000). |
| A21 | **Preceptor** | **Comunicados**: el selector global de materia **no se aplica** al preceptor. El preceptor debe ver comunicados de todas las materias sin importar el filtro, pero debe poder ver qué materia se especificó | `Preceptores/` (sección comunicados) | El preceptor ve todos los comunicados sin filtro de materia, pero el tag de materia es visible | ✅ Verificado: backend (`_comunicado_visible_para_ctx`) filtra solo por curso y el caso `preceptor` de `comunicadosFiltrados` en `Shared/ComunicadosView.jsx` no usa materia; `comunicadoMatchesCurso` ignora `hasMateria`. La columna "Materia asignada" toma el `id_materia` del alcance del comunicado. Vía API como preceptor del 1°1: el comunicado específico por materia ("Prueba - 1°1 Practicas del lenguaje", `id_materia`=1 en el alcance) figura en su lista; preceptores no pueden publicar (403, correcto). |
| A22 | **Director** | **Quitar rol admin**: al quitar el rol a alguien, sigue **apareciendo en la lista** de administradores (backend funciona, problema en frontend) | `Administracion/administradores.jsx` (fetchUsuarios / handleQuitarRol) | Que al quitar el rol, el usuario desaparezca de la lista | ✅ Verificado + arreglado: el listado `/api/usuarios/` solo devuelve usuarios con rol `admin`, por lo que `fetchUsuarios` refresca bien al quitar el rol. Casos validados por API: (1) quitar `admin` a un multi-rol → 200 y desaparece de la lista y de `con-rol admin`; (2) quitar a alguien con rol único → 400 "No se puede quitar el rol: la persona debe conservar al menos un rol." (regla de negocio). El `QuitarRolModal` ya filtra personas con `cantidad_roles > 1`. Se corrigió que los toasts de `handleQuitarRol`/`handleAgregarRol` no mostraban la verdadera causa (el backend devuelve la clave `error`, no `detail`): ahora leen `data.error || data.detail`. |
| A23 | **Responsive** | Los **desplegables** no se abren en mobile (no entran en la pantalla) | `Shared/` (selectores/dropdowns), todos los componentes con desplegables | Que los desplegables se adapten al tamaño de pantalla móvil | ✅ Arreglado + build OK: en mobile (`max-width: 768px`) había **dos media queries que competían** por el sidebar (líneas ~2183 y ~3636 de `index.css`); la de 2183 ocultaba los `span` de los botones del menú (`display: none`), por lo que al desplegar un submenú solo se veían los iconos, y el `.sidebar-submenu` (posicionado `absolute; bottom: 100%`) se abría sin límite de altura y quedaba cortado por la proporción de la pantalla. Se corrigió en el bloque mobile final: `.sidebar-submenu` ahora tiene `max-height: 70vh` con `overflow-y: auto` y scroll táctil, y `.sidebar-submenu-btn span` vuelve a mostrarse (`display: inline !important`) para que las opciones del desplegable se lean en pantalla chica. Precisión del usuario: "se despliegan pero en el menú mobile no se ven por la proporción de la resolución". |
| A24 | **Preceptor** | **Quitar rol docente**: el botón funciona pero el docente **sigue apareciendo en la lista** y debe seguir teniendo la relación con la materia activa | `Preceptores/docentes.jsx` (handleQuitarRol) | Que al quitar el rol, el docente desaparezca de la lista | ✅ Arreglado + verificado: el backend `quitar-rol` elimina solo el rol (200) y **no borra el perfil ni las relaciones `curso_materia`** (verificado por API: el perfil sigue en `/api/docentes/` y las materias activas quedan intactas). El panel ahora oculta en la sesión al docente cuyo rol se quitó (`idsDocentesSinRol` se excluye de la lista y se reincorpora si se vuelve a asignar el rol). Además faltaba definir `mensajeError` en este componente (los errores crasheaban en silencio al no estar declarada): se agregó leyendo `data.error || data.detail || data.message`. El `QuitarRolModal` ya ocultaba personas con un solo rol. |
| A25 | **Preceptor** | **Lógica adelanto de horas**: no debería poder crear un adelanto de horas a un horario/día en el que hay **otra clase que no se suspendió** ni se informó la falta del profesor | `Shared/AdelantosHoras.jsx` o backend | Validación: solo permitir adelanto si el horario está afectado por falta/suspensión | ✅ Arreglado + verificado en backend: `AdelantoHorasViewSet.perform_create/perform_update` ahora validan el contexto antes de guardar. Regla 1 (origen): el bloque original de la materia según el horario de ese día de la semana debe estar afectado por una **suspensión institucional** (evento que cubre la franja, excluye "No se cancelan las clases") o por **falta del docente** registrada (`AsistenciaDocente` 'Ausente' en esa fecha). Si no, 400: "No se puede autorizar el adelanto: la clase de esa materia no está afectada por una suspensión ni se informó la falta del docente para esa fecha." Regla 2 (destino): la franja elegida no puede chocar con **otra clase activa** del mismo curso que no esté suspendida ni tenga falta; si choca, 400: "El horario elegido coincide con la clase de Educación Artística en ese curso, que no está suspendida ni tiene falta del docente registrada." Helpers: `_evento_cubre_franja`, `_bloque_afectado`, `_validar_contexto_adelanto`. Prueba vía API como `admin_prueba` (curso 1, materia 1, docente 14, lunes): sin suspensión ni falta → 400; con suspensión todo el día → 201; franja destino 09:55-11:55 con suspensión parcial (solo cubría 07:30-09:30) → 400 (choca con Ed. Artística); misma franja que la clase original → 201; con falta registrada (martes) → 201. Cleanup OK. |
| A26 | **Admin** | **Calendario institucional**: falta opción de **"No se cancelan las clases"** (evento que no implica suspensión) | `Administracion/CalendarioInstitucional.jsx` | Checkbox "No se cancelan las clases" al crear evento | ✅ Arreglado + verificado: se agregó el tipo de evento **"No se cancelan las clases"** en `TIPO_EVENTO_CHOICES` y `PRIORIDAD_MAP` (models.py), en `TIPOS`/`TIPO_COLORS` + leyenda (CalendarioInstitucional.jsx) y en el dict de `_notificar_evento_institucional`. La columna `tipo_evento` en MySQL era un ENUM con los 4 valores viejos → hubo que ampliarla con `ALTER TABLE ... MODIFY COLUMN` (agregar el valor no toca datos). Verificado por API: `POST /api/eventos-institucionales/` con `tipo_evento='No se cancelan las clases'` → **201**. |

---

## MEDIA — Funciona parcialmente / notificación faltante

| # | Rol | Problema | Nota | Estado |
|---|---|---|---|---|
| M1 | Alumno/Familia | No llega notificación de **actas de curso** (E21) | Verificar que el segmento incluye "alumno"/"familia" en el envío de actas de curso | ✅ Arreglado + verificado: `_notificar_acta_curso` ahora recorre a los alumnos del curso y los notifica con `notificar_alumno` (alumno + familias). Test real: acta de curso en 1°1 notificó al alumno y a sus familias. |
| M2 | Alumno/Familia | No llega notificación de **promoción** (E13) | Pendiente de testear (no hubo caso de prueba) | ✅ Verificado: `_notificar_consolidacion` (academico.py) notifica al alumno y familias ("Materia aprobada") al consolidar la promoción. Test real OK. |
| M3 | Alumno/Familia | No llega notificación de **recursadas** (E18) | Pendiente de testear | ✅ Verificado: `_notificar_recursada` notifica al alumno y familias ("Resultado de recursada"). Test real OK (filtrando por id_notificacion > max_id, ojo con dedup anti-spam). |
| M4 | Alumno/Familia | No llega notificación de **bloqueo de horario** (E19). Familia: "no sé qué sería" | Aclarar a qué se refiere este evento | ❌ Pendiente aclaración |
| M5 | Alumno | Notificación de **evento institucional**: debería decir **"Nuevo evento institucional: NombreDelEvento"** en vez del nombre solo | `notifications.py` (formato del título en `_notificar_evento_institucional`) | ✅ Arreglado: `_notificar_evento_institucional` ahora titula `Nuevo evento institucional: <tipo>` (antes el título era solo el tipo). Verificado: con el evento de prueba del tipo "No se cancelan las clases", la notificación sale como `Nuevo evento institucional: No se cancelan las clases` con mensaje "No se cancelan las clases el 11/09/2026 ...". |
| M6 | Alumno/Familia | No hay notificación de **actividad de materia adeudada** | Verificar si existe el caso de prueba | ✅ Verificado: `_notificar_actividad_adeudada` notifica al alumno y familias al crear una `ActividadMateriaAdeudada` (tipo INTENSIFICACION/PREVIA; usar este modelo, no `ActividadDocente`). Test real OK. |
| M7 | Familia | No llega notificación de **suplencia** (E17) | Verificar el segmento "familia" en la notificación de suplencia | ✅ Verificado: `_notificar_suplencia_asignada` notifica a los estudiantes del curso y sus familias a través de `notificar_alumno`. Test real OK. |
| M8 | Docente | **Notificaciones que NO debería recibir**: le llegan: nuevo usuario (preceptor/alumno/docente), asistencias cargadas (propias), planificación para revisión (propia), notas cargadas/actualizadas (propias), falta de docente, cambio de estructura. | Son notificaciones de otros segmentos que le llegan por el bug multi-rol (A1). Con A1 resuelto, estas deberían dejar de llegar al operar como docente. | ✅ Cubierto por A1 |
| M9 | Preceptor | **Notificación de acta**: el título dice "acta de evaluación" cuando es de curso (debería decir "Acta de curso") | `notifications.py`: revisar el título de la notificación de acta de curso | ✅ Arreglado + verificado: `_notificar_acta_curso` usa título fijo `Acta de curso` (ya no `f'Acta de {tipo}'`). Test real: salió como "Acta de curso". |
| M10 | Preceptor | **Notificación de suplencia**: no aparece en notificaciones | Verificar si el preceptor está en el segmento de destinatarios de suplencia | ✅ Verificado: `_notificar_suplencia_asignada` ya notificaba al preceptor del curso. Test real OK (recordar: con rol "docente" el usuario no ve notifs de segmento "preceptor", cubierto por A1). |
| M11 | Preceptor | **Notificación de cambios de horario**: no aparece | Verificar el segmento "preceptor" en la notificación de horario | ✅ Verificado: `_notificar_cambio_horario` ya notificaba al preceptor del curso. Test real OK. |
| M12 | Preceptor | **Notificaciones excesivas**: llegan demasiadas notificaciones (notas cargadas/actualizadas, asistencias, prenota, intensificación, materia adeudada, rendición) | Revisar si la deduplicación anti-spam está funcionando para estos eventos | ✅ Revisado: el anti-spam CONTENT (5 min, contenido idéntico) y DAILY (una por día) SÍ funcionan en `notifier`. Los límites globales (50/día, 10/hora) están **desactivados** por decisión del cliente (TODO en `notifications.py` líneas ~130-134). El "exceso" viene de emisores que notifican al preceptor por cada carga sin `dedupe_key` ni suma diaria (una notif por materia/día). Decisión de producto pendiente: reactivar límites y/o agrupar por día. |
| M13 | Jefe | **Actas de curso**: las de alumnos y cursos se ven iguales (mismo estilo/título) | Diferenciar visualmente actas de curso (E21) de actas de alumno (E4) | ✅ Arreglado + verificado: `_notificar_acta_curso` ahora se abstiene si el acta ya tiene un `ActaAlumno` asociado (caso E4). Antes, al crear un acta de alumno el frontend también creaba `ActaCurso` y se spameaba a TODO el curso con "Acta de curso". Además `_es_tipo_acta_conducta` ahora reconoce "Informe de conducta" (tipo seed) por substring, no solo Apercibimiento/Conducta/Amonestación/Sanción. Test: E4 → 0 notifs de "(acta de) curso", solo la de conducta (alumno + 4 familias + preceptor + jefe + directivos); E21 → notifica a todo el curso normalmente. |
| M14 | Jefe | No llega notificación de **suplencia** | Ídem M10 | ✅ Arreglado + verificado: `_notificar_suplencia_asignada` ahora también notifica a los usuarios con rol `jefe_preceptores` (SEGMENTO_JEFE_PRECEPTORES). Test real OK. |
| M15 | Jefe | No llega notificación de **cambios de horario** | Ídem M11 | ✅ Arreglado + verificado: `_notificar_cambio_horario` ahora también notifica a los usuarios con rol `jefe_preceptores`. Test real OK. |
| M16 | Jefe | Notificación de **"docentes"** no llega | Verificar a qué se refiere el usuario (¿docente asignado? ¿docente titular?) | ❌ Pendiente aclaración |
| M17 | Admin | No llega notificación de **suplencia** (E17) | Verificar segmento "directivo" en notificación de suplencia | ✅ Verificado: `_notificar_suplencia_asignada` notificaba a `_usuarios_directivos()` (admin incluido). Test real OK. |
| M18 | Admin | No llega notificación de **cambio de estructura** (curso/materia creado/modificado/eliminado) | Verificar segmento "directivo" en notificación de estructura | ✅ Arreglado + verificado: `_notificar_cambio_estructura` notifica a `_usuarios_directivos()`. Creó 8 notifs (admin + directivos). Ojo: el filtro por `id > max_id` previo puede dar vacío por dedup anti-spam (5 min); el micro-test aislado con mensaje único confirmó la creación. |
| M19 | Admin | No llega notificación de **actas** (E21/E4) | Verificar segmento "directivo" en notificación de actas | ✅ Arreglado + verificado: `_notificar_acta_curso` y `_notificar_acta_conducta` ahora notifican a `_usuarios_directivos()` (SEGMENTO_DIRECTIVO). Test real OK: E21 y E4 llegan a los directivos. |
| M20 | Director | Igual que admin (M17-M19) + quitar rol sigue en lista (A22) | Verificar todos los segmentos de notificación para director | ✅ Cubierto por M17-M19 (director está en `_usuarios_directivos()`) + A22 (lista de administradores en frontend ya corregida). |

---

## BAJA — Cosas que faltan implementar / mejorar

| # | Rol | Problema / Mejora | Dónde | Qué se espera | Estado |
|---|---|---|---|---|---|
| B1 | Preceptor | **Libro de temas**: al apretar "Ver" en gestión diaria redirige a Actividades en vez de un módulo propio de Libros de Temas | `Preceptores/GestionDiaria.jsx` | Crear sección "Libros de temas" en gestión diaria que muestre las materias del día (prioridad) y todas las demás | ✅ Resuelto: creado `Preceptores/LibroTemasPreceptor.jsx` (vista de solo lectura, materias del día primero) y en `GestionDiaria.jsx` el botón "Ver" navega a `libro-temas` (antes `actividades`). Se agregó el ítem "Libro de temas" al sidebar del preceptor y el case en `PreceptorDashboard.jsx`. Build OK. |
| B2 | Preceptor | **Asistencia docente**: falta poder marcar **con anticipación** si un docente va a faltar un día específico. Debería mostrar primero los del día de hoy, y reflejar adelantos de horas | `Preceptores/asistencias.jsx` | Marcar ausencia docente anticipada + orden por fecha + reflejar adelantos | ✅ Resuelto: backend `docentes_disponibles` acepta `?fecha=` (default hoy), devuelve **todo el día** ordenando hoy primero, con `es_hoy` y `es_adelanto`; `registrar_asistencia_docente` acepta `fecha` opcional: rechaza fechas pasadas, permite registro anticipado (primer bloque del día) para fechas futuras, solo valida evento institucional para hoy. Frontend `asistencias.jsx`: selector de fecha, banner de "hoy", en fechas futuras solo se ofrece `Ausente`, badge "Adelanto" y registro envía la fecha. Build OK. |
| B3 | Preceptor | **Libro de temas**: el preceptor no carga libros, solo ve los que subieron los docentes | `Preceptores/GestionDiaria.jsx` | Vista de solo lectura de libros de temas subidos por docentes | ✅ Resuelto: `Preceptores/LibroTemasPreceptor.jsx` muestra en solo lectura los libros subidos por los docentes de las materias del día (y el resto). Build OK. |
| B4 | Jefe | **Estadísticas**: borrar texto "Datos actualizados en tiempo real..." y verificar si hay más datos relevantes | `JefePreceptores/EstadisticasPreceptoria.jsx` | Limpiar texto redundante + agregar datos si son relevantes | ✅ Resuelto: eliminado el texto redundante de `EstadisticasPreceptoria.jsx`. Build OK. |
| B5 | Jefe | **Supervisión de Preceptores**: sección no existe, hay que implementarla | Nuevo componente `JefePreceptores/SupervisionPreceptores.jsx` | Mostrar tareas diarias del preceptor (tomar asistencia de docentes, etc.) | ✅ Resuelto: el componente `JefePreceptores/SupervisionPreceptores.jsx` ya existía con backend (`/supervision-preceptores/`, views.py). Se integró en `JefePreceptorDashboard.jsx` (case `supervision-preceptores`) y en el sidebar del jefe (ítem "Supervisión de preceptores" bajo Jefatura). Build OK. |
| B6 | Jefe | **Estudiantes**: falta **buscador por filtro** como en los demás roles | `JefePreceptores/` (sección estudiantes) | Agregar buscador por nombre, DNI, curso | ✅ Resuelto: el buscador de `Preceptores/alumnos.jsx` estaba oculto con `!readOnly`; el jefe usa el componente con `readOnly`, así que no veía el buscador. Se quitó la condición y el buscador ahora es visible también en modo lectura. Build OK. |
| B7 | Jefe | **Perfil**: borrar texto "Supervisor general de la actividad preceptoría..." y quitar uno de los dos "Activo" duplicados | `JefePreceptores/JefePreceptorDashboard.jsx` o panel de perfil | Limpiar perfil | ✅ Resuelto: eliminado el info-box "Supervisor general..." y uno de los dos StatCard "Estado de la cuenta" (Activo duplicado) en `PanelJefePreceptor.jsx`. Build OK. |
| B8 | Jefe | **Asignación de cursos**: la lista muestra **más personas** que en "Administrar preceptores" | `JefePreceptores/AsignacionCursos.jsx` vs `AdministracionPreceptores.jsx` | Que ambas listas muestren los mismos preceptores | ✅ Resuelto: `AsignacionCursos.jsx` usaba `preceptores` de DataContext, que llama a `getPreceptores()` sin rol e incluye los perfiles de jefes; `AdminPreceptores` usa `getPreceptores('preceptor')`. Se cambió `AsignacionCursos.jsx` para hacer fetch local de `getPreceptores('preceptor')` y mapear `id_preceptor→id`, `cursos_asignados→cursos`. Build OK. |
| B9 | Jefe | **Asistencia por docentes**: debería ser como en preceptores (con los cambios de B2), pero en jefe es **solo visual** | `JefePreceptores/` (sección asistencias) | Vista de asistencia docentes igual a preceptor pero sin botones de edición | ✅ Resuelto: el jefe usa `<Asistencias readOnly />`, que con los cambios de B2 incluye el selector de fecha sin mostrar botones de edición/registro. Build OK. |
| B10 | Alumno/Jefe/Admin | **Comunicados**: falta mostrar el **autor** del comunicado | `AlumnoDashboard.jsx` (comunicados), `JefePreceptores/ComunicadosJefe.jsx`, `Administracion/comunicados.jsx` | Mostrar nombre del autor en el listado/detalle | ✅ Resuelto (cubierto por A20): `ComunicadosView.jsx` muestra el autor con `creador_nombre` (fallback `getNombreAutor`), usado por Alumno/Familia/Preceptor/Jefe/Docente; `Administracion/comunicados.jsx` también muestra `creador_nombre`. El backend lo expone vía `ComunicadoSerializer`/`ActaSerializer` (serializers.py ~2004/2077/2175). |
| B11 | Preceptor | **Admin no recibe notificación** al preceptor crear un alumno nuevo | `notifications.py` (evento E20 / `_notificar_carga_usuario`) | Que admin reciba la notificación de alta de usuario | ✅ Verificado: `_notificar_carga_usuario` (E20) en `AlumnoViewSet.perform_create` notifica a los directivos (`admin`/`director`) cuando el preceptor crea un alumno. No se requirieron cambios. |
| B12 | Admin | **Carga única**: el usuario no sabe a qué se refiere esta funcionalidad. Verificar si está implementada y es clara | `Administracion/` (sección carga única) | Documentar o implementar la carga única de manera que sea evidente | ✅ Resuelto: la funcionalidad existe en `Preceptores/GestionDiaria.jsx` (no hay sección propia en Admin): el preceptor notifica al docente para que cargue asistencias y libro de temas con plazo de 20 minutos. El término confuso se aclaró en la UI: botón renombrado a **"Recordar carga"** (tooltip: "Recordar al docente cargar asistencias y libro de temas — plazo de 20 minutos"), texto de ayuda explicando el flujo y banner "Notificación enviada — el docente cargará en X min". Build OK. |
| B13 | Performance | **Calificaciones tardan mucho** en guardar (docente) | `Profesores/PanelAlumnos.jsx` (guardado de calificaciones) | Optimizar el guardado de calificaciones para que sea más rápido | ✅ Resuelto: el guardado hacía ~2N peticiones HTTP individuales (create/update por alumno y período). Se agregó el endpoint `POST /calificaciones/batch/` (`CalificacionViewSet.batch`) que crea/actualiza todas las notas en UNA petición, verifica el docente activo una sola vez por materia y deduplica notificaciones por materia+período. `PanelAlumnos.handleGuardar` ahora usa `guardarCalificacionesBatch(items)`. Build OK. |
| B14 | Performance | **Asistencias tardan mucho** en cargarse (docente) | `Profesores/PanelAsistencia.jsx` (carga de asistencias) | Optimizar la carga de asistencias | ✅ Resuelto: `cargarServerTime` (endpoint `server-time/` con cálculo de horario/contexto, pesado) dependía de `fechaSeleccionada`, por lo que se re-ejecutaba en CADA cambio de fecha. Ahora solo se llama al montar o al cambiar de materia (`useCallback` con dep `cursoMateriaId`), evitando la petición redundante. La carga de asistencias por fecha sigue siendo una única petición filtrada. Build OK. |
| B15 | Performance | **Crear/editar alumno tarda bastante** (preceptor) | `Preceptores/alumnos.jsx` | Optimizar la creación/edición de alumnos | ✅ Resuelto: `AlumnoSerializer.validate_dni` recorría **todos** los alumnos en Python (`for alumno in Alumno.objects.all()`) en cada alta/edición (O(n) sin usar índice). Ahora usa `Alumno.objects.filter(dni=value).exclude(...)` (O(1), indexado), preservando el mismo comportamiento de unicidad. Build OK. |

---

## RESUMEN POR COMPONENTE ARCHIVO

### Frontend — Archivos a modificar

| Archivo | Issues | Prioridad | Estado |
|---|---|---|---|
| `Notificaciones.jsx` | C1 (verificar frontend), M5 | CRÍTICO | ✅ |
| `Preceptores/tutores.jsx` | C2, A10 | CRÍTICO | ✅ |
| `Preceptores/docentes.jsx` | C3, A24 | CRÍTICO | ✅ |
| `JefePreceptores/AdministracionPreceptores.jsx` | C4, A18 | CRÍTICO | ✅ |
| `Administracion/preceptores.jsx` | C4, A14 | CRÍTICO | ✅ |
| `Administracion/notas.jsx` | C7 (boletines selector) | CRÍTICO | ✅ |
| `Profesores/PanelPlanif.jsx` | C5, A5 | CRÍTICO | ✅ |
| `Profesores/PanelActividades.jsx` | C6 | CRÍTICO | ✅ |
| `Preceptores/Proyectos.jsx` | C6 | CRÍTICO | ✅ |
| `Shared/FiltrosAnioCurso.jsx` | C7 (causa raíz del selector) | CRÍTICO | ✅ |
| `AlumnoDashboard.jsx` | A2, M1-M6 | ALTA | ✅ |
| `Familia/Asistencias.jsx` | A2 | ALTA | ✅ |
| `Profesores/PanelInfo.jsx` | A3 | ALTA | ✅ |
| `Profesores/PanelMateriasAdeudadasDocente.jsx` | A4 | ALTA | ✅ |
| `Profesores/PanelAlumnos.jsx` | A7, B13 | ALTA | ✅ |
| `Preceptores/` (horarios) | A8 | ALTA | ✅ |
| `Preceptores/actas.jsx` | A9 | ALTA | ✅ |
| `Preceptores/alumnos.jsx` | A11, B15 | ALTA | ✅ |
| `Administracion/alumnos.jsx` | A12 | ALTA | ✅ |
| `Administracion/docentes.jsx` | A13 | ALTA | ✅ |
| `Administracion/cursos.jsx` | A15 | ALTA | ✅ |
| `Administracion/asistencias.jsx` | A16 | ALTA | ✅ |
| `Administracion/CalendarioInstitucional.jsx` | A26 | ALTA | ✅ |
| `JefePreceptores/AsignacionCursos.jsx` | A17, B8 | ALTA | ✅ |
| `JefePreceptores/ComunicadosJefe.jsx` | A20 | ALTA | ✅ |
| `Administracion/administradores.jsx` | A22 | ALTA | ✅ |
| `Shared/AdelantosHoras.jsx` | A25 | ALTA | ✅ |
| `Shared/` (dropdowns) | A23 | ALTA | ✅ |
| `Preceptores/asistencias.jsx` | B2 | BAJA | ✅ |
| `Preceptores/GestionDiaria.jsx` | B1, B3, B12 | BAJA | ✅ |
| `Preceptores/LibroTemasPreceptor.jsx` | B1, B3 (nuevo) | BAJA | ✅ |
| `JefePreceptores/EstadisticasPreceptoria.jsx` | B4 | BAJA | ✅ |
| `JefePreceptores/JefePreceptorDashboard.jsx` | B5, B7 | BAJA | ✅ |
| `JefePreceptores/SupervisionPreceptores.jsx` | B5 (nuevo) | BAJA | ✅ |
| `Profesores/PanelAsistencia.jsx` | B14 | BAJA | ✅ |
| `Administracion/comunicados.jsx` | B10 | BAJA | ✅ |
| `services/api.js` | C5, C6, B13 | CRÍTICO | ✅ |

### Backend — Archivos a modificar

| Archivo | Issues | Prioridad | Estado |
|---|---|---|---|
| `notifications.py` | A1, M1-M19, M5 | ALTA | ✅ |
| `views.py` (NotificacionViewSet) | A1, A22, M8 | ALTA | ✅ |
| `views.py` (quitar_rol) | A22 | ALTA | ✅ Backend OK |
| `views.py` (CalificacionViewSet.batch) | B13 | BAJA | ✅ |
| `views.py` (docentes_disponibles, registrar_asistencia_docente) | B2 | BAJA | ✅ |
| `views.py` (supervision_preceptores) | B5 | BAJA | ✅ Ya existía |
| `serializers.py` | C4, A11, A20, B15 | CRÍTICO | ✅ Hecho |
| `permissions.py` | A8 | ALTA | ✅ Hecho |
| `models.py` (Notificacion.rol) | A1 | ALTA | ✅ Migración + campo |
| `models.py` (tipo_evento "No se cancelan las clases") | A26 | ALTA | ✅ |
| `models.py` (ddjj_docente.verificada) | A6 | ALTA | ✅ Migración 0005 |
| Rutas de archivos (media/) | C5, C6 | CRÍTICO | ✅ |

---

## CONTADOR

| Prioridad | Total | Hecho ✅ | Pendiente ❌ |
|---|---|---|---|
| Crítico | 7 | 7 | 0 |
| Alta | 26 | 26 | 0 |
| Media | 20 | 18 | 2 |
| Baja | 15 | 15 | 0 |
| **Total** | **68** | **66** | **2** |

> Pendientes: **M4** (bloqueo de horario, espera aclaración de a qué evento se refiere) y **M16** (notificación de "docentes" al jefe, espera aclaración).
